<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>Anagrams</title>
  </head>
  <body>
    <div class="container">
      <h1>Anagrams!</h1>
      <h3>These words are anagrams of @Model.GetBaseWord():</h3>
      <ul>
        @foreach (string word in @Model.GetAnagramList())
        {
          <li>@word</li>
        }
      </ul>
      <h3>These words are not anagrams of @Model.GetBaseWord():</h3>
      <ul>
        @foreach (string word in @Model.GetNotAnagramList())
        {
          <li>@word</li>
        }
      </ul>
      <p><a href="/">Test more anagrams</a></p>
    </div>
  </body>
</html>
